#!/bin/bash
# Monitor output from arduino

PORT=/dev/ttyUSB${1:-1}

clear
stty -F ${PORT} 115200
echo Monotor port ${PORT}

# cat < ${PORT}
# exit

while true
do
    read LINE
    echo $LINE
#   if [ "$LINE" != "" ]
#   then
#       echo
#   fi
done < ${PORT}


